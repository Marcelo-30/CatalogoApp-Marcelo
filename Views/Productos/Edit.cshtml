@model Producto
@{
    ViewData["Title"] = "Editar producto";
}

<div class="page-header">
    <div>
        <p class="eyebrow">Administracion</p>
        <h1>Editar producto</h1>
        <p class="page-subtitle">Actualiza la informacion visible en el catalogo.</p>
    </div>
    <a class="btn ghost" asp-action="Index">Volver</a>
</div>

<section class="form-shell">
    <form asp-action="Edit" method="post" class="form">
        <input type="hidden" asp-for="Id" />
        <input type="hidden" asp-for="FechaRegistro" />
        <partial name="_ProductoForm" model="Model" />
        <div class="actions form-actions">
            <button class="btn primary" type="submit">Actualizar producto</button>
            <a class="btn ghost" asp-action="Index">Cancelar</a>
        </div>
    </form>
</section>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
