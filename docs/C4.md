# Modelo C4 de CatalogoRopaMVC

Este documento representa la arquitectura final comprobada en el repositorio. Los diagramas usan `flowchart` de Mermaid para que GitHub pueda renderizarlos. Las etiquetas **Persona**, **Sistema**, **Contenedor**, **Componente** y **Base de datos** indican el tipo de elemento C4.

## Nivel 1 — Contexto del sistema

**Propósito:** explicar quién usa CatalogoRopaMVC, qué objetivo cumple y con qué sistemas externos reales se relaciona, sin detallar su implementación interna.

CatalogoRopaMVC publica un catálogo de ropa para visitantes y permite al vendedor dueño administrar inventario. GitHub entrega cambios validados a Azure y el navegador obtiene imágenes desde las URL externas guardadas en el catálogo.

```mermaid
flowchart LR
    visitante["Persona: Cliente o visitante<br/>Consulta, filtra y revisa prendas"]
    vendedor["Persona: Vendedor dueño<br/>Inicia sesión y administra el catálogo"]
    sistema["Sistema: CatalogoRopaMVC<br/>Frontend React completo y API REST"]
    imagenes["Sistema externo: Recursos de imágenes<br/>URLs públicas, actualmente Unsplash"]
    github["Sistema externo: GitHub<br/>Repositorio y GitHub Actions"]
    azure["Sistema externo: Microsoft Azure<br/>Hospedaje administrado y datos SQL"]

    visitante -->|"Consulta catálogo y endpoints GET"| sistema
    vendedor -->|"Registra la cuenta, inicia sesión y administra"| sistema
    sistema -->|"Entrega URLs guardadas"| imagenes
    visitante -.->|"El navegador descarga imágenes"| imagenes
    github -->|"Valida y despliega una versión"| azure
    azure -->|"Ejecuta y publica"| sistema
```

## Nivel 2 — Contenedores

**Propósito:** mostrar las unidades de ejecución y almacenamiento, cómo se comunican y dónde se despliegan. React se ejecuta en el navegador y sus archivos se sirven desde ASP.NET Core; no se despliega como servicio independiente.

El API ASP.NET Core y el bundle compilado de React se publican como una sola imagen en Azure Container Apps. React cubre la experiencia pública y administrativa. La aplicación usa Azure SQL Database mediante EF Core. GitHub Actions y GHCR forman la vista complementaria de entrega, no una dependencia requerida para atender cada solicitud.

```mermaid
flowchart LR
    cliente["Persona: Cliente o visitante"]
    vendedor["Persona: Vendedor dueño"]
    navegador["Contenedor: Frontend React en navegador<br/>React Router, Fetch API, HTML y CSS"]

    subgraph nube["Microsoft Azure"]
        webapp["Contenedor: Backend y servidor web<br/>ASP.NET Core 8 API REST + estáticos<br/>Azure Container Apps Consumption"]
        sql[("Base de datos: Azure SQL Database<br/>Catálogo, relaciones y vendedor dueño")]
    end

    imagenes["Sistema externo: Recursos de imágenes<br/>URLs públicas"]

    subgraph entrega["Vista complementaria de entrega"]
        repo["Contenedor: Repositorio GitHub"]
        actions["Contenedor: GitHub Actions<br/>CI, imagen y despliegue a production"]
        registry["Contenedor: GHCR público<br/>Imagen inmutable por SHA"]
    end

    cliente -->|"Usa"| navegador
    vendedor -->|"Usa"| navegador
    navegador -->|"HTTPS: bundle React, lecturas y escrituras /api"| webapp
    webapp -->|"EF Core sobre conexión cifrada"| sql
    webapp -->|"Incluye URL de imagen en HTML o JSON"| navegador
    navegador -.->|"HTTPS"| imagenes
    repo -->|"Push o pull request"| actions
    actions -->|"Publica imagen validada"| registry
    actions -->|"Actualiza revisión por OIDC"| webapp
    registry -->|"Descarga anónima de imagen pública"| webapp
```

## Nivel 3 — Componentes

**Propósito:** detallar los componentes reales dentro del navegador y el contenedor ASP.NET Core, además de sus principales dependencias. No se representa una capa de repositorios porque no existe en el código.

React organiza páginas públicas y administrativas, componentes, hooks y una capa HTTP con antiforgery. Los controladores API entregan DTOs, autentican al vendedor y protegen las mutaciones. El servicio de autenticación encapsula credenciales, la fábrica centraliza el mapeo REST y `ApplicationDbContext` concentra el acceso a SQL Server.

```mermaid
flowchart TD
    subgraph navegador["Contenedor: Frontend React en navegador"]
        spa["Componente: SPA React<br/>Catálogo, autenticación, panel y CRUD"]
        client["Componente: capa HTTP<br/>Fetch, antiforgery, AbortController y errores"]
        ui["Componente: UI reutilizable<br/>Navegación, formularios, tarjetas y estados"]
        spa --> ui
        spa --> client
    end
    clienteApi["Contenedor externo: Cliente HTTP"]
    sql[("Base de datos: Azure SQL Database")]

    subgraph app["Contenedor: CatalogoRopaMVC en Azure Container Apps"]
        program["Componente: Program.cs<br/>DI, rutas, cookies, autorización,<br/>proxy, health checks y configuración"]

        subgraph presentacion["Entrada HTTP"]
            api["Componente: Controladores API<br/>Productos, catálogos y autenticación"]
            antiforgery["Componente: Antiforgery<br/>Cookie segura + encabezado X-CSRF-TOKEN"]
            dtos["Componente: DTOs y modelos de entrada<br/>Producto, categoría, color y credenciales"]
        end

        subgraph aplicacion["Servicios de aplicación"]
            auth["Componente: VendedorAuthService<br/>Registra y valida al vendedor dueño"]
            factory["Componente: ProductoDtoFactory<br/>Construye respuestas del API"]
            hasher["Componente: PasswordHasher de Vendedor<br/>Hash y verificación de contraseña"]
        end

        subgraph persistencia["Dominio y persistencia"]
            dbcontext["Componente: ApplicationDbContext<br/>DbSet, relaciones, índices y datos semilla"]
            entities["Componente: Entidades<br/>Producto, Categoría, Talla, Color,<br/>ImagenProducto y Vendedor"]
            migrations["Componente: Migraciones EF Core<br/>InitialCreate y AgregarVendedorUnico"]
        end
    end

    client -->|"JSON, cookie y token antiforgery"| api
    clienteApi -->|"Solicitudes REST"| api
    program -->|"Registra y configura"| api
    program -->|"Configura autenticación y autorización"| auth
    program -->|"Configura"| antiforgery
    api -->|"Valida mutaciones con"| antiforgery
    api -->|"Registra o autentica"| auth
    api -->|"Valida entradas y devuelve respuestas"| dtos
    api -->|"Mapea productos"| factory
    api -->|"Consulta y modifica"| dbcontext
    auth -->|"Usa"| hasher
    auth -->|"Consulta y guarda vendedor"| dbcontext
    factory -->|"Lee propiedades y navegaciones"| entities
    dbcontext -->|"Mapea"| entities
    migrations -->|"Evoluciona esquema"| dbcontext
    dbcontext -->|"Microsoft.Data.SqlClient mediante EF Core"| sql
```

## Notas de consistencia

- LocalDB se conserva únicamente para desarrollo; el despliegue usa Azure SQL Database.
- Frontend estático y API comparten el mismo proceso y revisión de Container Apps.
- React se compila en CI/Docker, se entrega dentro de `wwwroot` y se ejecuta únicamente en el navegador.
- React atiende catálogo, autenticación y CRUD; no quedan Razor Views ni controladores MVC convencionales.
- Las imágenes son referencias URL; el proyecto no contiene almacenamiento de blobs.
- GitHub Actions entrega la aplicación, pero una falla de GitHub no impide atender una versión ya desplegada.
- Todas las escrituras autenticadas con cookie exigen antiforgery; el control se evalúa en `ATAM.md`.
